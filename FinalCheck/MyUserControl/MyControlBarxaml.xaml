<UserControl x:Class="FinalCheck.MyUserControl.MyControlBarxaml"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinalCheck.MyUserControl"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Name="MyControlBarTA"
             Background="{DynamicResource MaterialDesignPaper}"
             
             FontFamily="{DynamicResource MaterialDesignFont}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding cmd_MoveWindow}" CommandParameter="{Binding ElementName=MyControlBarTA}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <!--<UserControl.DataContext>
        <Mydata:ControlBarViewModelcs></Mydata:ControlBarViewModelcs>
    </UserControl.DataContext>-->
    <Grid>
        <materialDesign:ColorZone Padding="0" Background="LightGray">
            <Grid Margin="5,0,5,0">
                <StackPanel DockPanel.Dock="Right" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Background="Transparent">
                    <Button Foreground="Black" Height="15" Width="40" Command="{Binding cmd_MinimizeWindow}" CommandParameter="{Binding ElementName=MyControlBarTA}" BorderBrush="Transparent" Margin="5" Padding="0" Background="Transparent" Content="{materialDesign:PackIcon Kind=WindowMinimize}" />
                    <Button Foreground="Black"  Height="16" Width="40" Command="{Binding cmd_NormalWindow}" CommandParameter="{Binding ElementName=MyControlBarTA}" BorderBrush="Transparent" Margin="5" Padding="0" Background="Transparent" Content="{materialDesign:PackIcon Kind=WindowRestore}" />
                    <Button Foreground="Black"  Height="16" Width="40" Command="{Binding cmd_CloseWindow}" CommandParameter="{Binding ElementName=MyControlBarTA}" BorderBrush="Transparent" Margin="5" Padding="0" Background="Transparent" Content="{materialDesign:PackIcon Kind=CloseThick}" />
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
