<Window x:Class="FinalCheck.Setup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalCheck"
        mc:Ignorable="d"
         xmlns:uc="clr-namespace:FinalCheck.MyUserControl"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Name="MyControlBarTA"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
         WindowStyle="None"
        ResizeMode="NoResize"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        Title="Setup" Height="280" Width="600" Closed="MyControlBarTA_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <!--<RowDefinition Height="50"></RowDefinition>-->
        </Grid.RowDefinitions>
        <uc:TitleBar Grid.Row="0">
        </uc:TitleBar>
        <Grid Grid.Row="1" Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Margin="5" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                   
                </Grid.RowDefinitions>
                <TextBlock HorizontalAlignment="Center" FontSize="25" FontWeight="Medium" Text="Setup Connection"></TextBlock>
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
                    <StackPanel VerticalAlignment="Center"  Orientation="Horizontal" Margin="0,0,20,0">
                        <Label FontWeight="Medium" FontSize="20">IP Address:</Label>
                        <TextBox FontSize="20" Text="192.168.0.0"></TextBox>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Center" Grid.Row="2" Orientation="Horizontal" Margin="0,0,20,0">
                        <Label FontWeight="Medium" FontSize="20">Port Number:</Label>
                        <TextBox FontSize="20" Text="8000"></TextBox>
                    </StackPanel>
                    <Button Background="Green" FontSize="15" Content="Test connect"></Button>
                </StackPanel>
                
            </Grid>
            <Grid Grid.Row="1" Margin="5" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    
                </Grid.RowDefinitions>
                <TextBlock HorizontalAlignment="Center" FontSize="25" FontWeight="Medium" Text="Setup Inspection"></TextBlock>
                <StackPanel HorizontalAlignment="Center" Grid.Row="1" Orientation="Horizontal">
                    <StackPanel  Orientation="Vertical" Margin="0,0,0,0">
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition1
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition2
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition3
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel  Orientation="Vertical" Margin="15,0,0,0">
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition4
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition5
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition6
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel  Orientation="Vertical" Margin="15,0,0,0">
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition7
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition8
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Left">
                            <TextBlock FontSize="20"  VerticalAlignment="Center"> Condition9
                            </TextBlock>
                            <ToggleButton Width="45" Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
               
                
                
            </Grid>
            
        </Grid>
       
    </Grid>

</Window>
